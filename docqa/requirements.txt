# Shared dependency set for DeepSeek-OCR, dots.ocr, olmOCR-2-7B-1025, and PaddleOCR-VL (HF loading path).
# Install with `uv add -r requirements.txt`.

torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0
transformers==4.55.2
tokenizers
einops
addict
easydict
git+https://github.com/rednote-hilab/dots.ocr.git@master#egg=dots-ocr
olmocr>=0.4.0

Pillow
pymupdf
python-docx
mlx-vlm
